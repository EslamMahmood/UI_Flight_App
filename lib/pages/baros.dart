import 'package:flutter/material.dart';

class BarosPage extends StatelessWidget {
  const BarosPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body:Center(
        child:Text('Baros'),
      ),
    );
  }
}