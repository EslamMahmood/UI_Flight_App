import 'package:flutter/material.dart';

class PalawanPage extends StatelessWidget {
  const PalawanPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body:Center(
        child:Text('Palawan'),
      ),
    );
  }
}